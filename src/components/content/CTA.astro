---
interface Props {
  title: string;
  body?: string;
  href: string;
  label: string;
}
const { title, body, href, label } = Astro.props;
---

<section class="cta-box">
  <h2>{title}</h2>
  {body && <p>{body}</p>}
  <a class="button" href={href}>{label}</a>
</section>
